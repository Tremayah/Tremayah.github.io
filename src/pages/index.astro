---
/*  "/" — the home page: the single-page app opening on the grid.

    The whole experience (grid, write-ups, more-works, behaviour) lives in the
    Landing component + site.ts. Each openable project also has its own
    deep-linkable page at /p/<slug>/ (see src/pages/p/[slug].astro), which renders
    this same app but boots straight into that project. */
import LandingLayout from '../layouts/LandingLayout.astro';
import Landing from '../components/Landing.astro';
---

<LandingLayout title="Raphael Murray-Browne">
  <Landing />
</LandingLayout>
